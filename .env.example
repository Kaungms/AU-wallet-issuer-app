# Local development: copy this file to .env.local.
# Vercel: set this variable in Project Settings > Environment Variables
# to your deployed NestJS API's public HTTP or HTTPS URL, then redeploy.
# HTTPS pages require HTTPS API access (directly or through a reverse proxy).
VITE_API_BASE_URL=http://localhost:3000
